---
version: 5
defaults:
  datadir: data          # This path is relative to hiera.yaml's directory.
  data_hash: yaml_data
hierarchy:
  - name: "FQDN"
    path: "fqdn/%{fqdn}.yaml"
  - name: "parameter_tests"
    path: "parameter_tests/%{parameter_tests}.yaml"
